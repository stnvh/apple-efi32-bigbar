# Ensure nvidia-persistenced's runtime dir exists with correct ownership at
# every boot. Without this a stale root-owned /run/nvidia-persistenced makes
# the daemon take SIGTERM right after registering the GPU.
# Install to /etc/tmpfiles.d/nvidia-persistenced.conf
d /run/nvidia-persistenced 0755 nvidia-persistenced nvidia-persistenced -
